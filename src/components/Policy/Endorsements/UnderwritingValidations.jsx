import React from 'react';

const UnderwritingValidations = props => (
  <aside className="underwriting-validation">
    <h4 className="uw-validation-header">Underwriting Validation</h4>
  </aside>
);

UnderwritingValidations.propTypes = {};

UnderwritingValidations.defaultProps = {};

export default UnderwritingValidations;
