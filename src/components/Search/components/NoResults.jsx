import React from 'react';
import PropTypes from 'prop-types';

function NoResults({ searchType }) {
  return (
    <div></div>
  );
};

NoResults.propTypes = {};

NoResults.defaultProps = {};

export default NoResults;
