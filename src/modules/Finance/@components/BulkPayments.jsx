import React, { useState, Fragment } from 'react';
import PropTypes from 'prop-types';

const BulkPayments = ({ auth }) => {
  return <section data-test="section-finance"></section>;
};

export default BulkPayments;
